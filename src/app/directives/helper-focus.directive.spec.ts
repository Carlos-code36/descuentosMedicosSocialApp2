import { HelperFocusDirective } from './helper-focus.directive';

describe('HelperFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new HelperFocusDirective();
    expect(directive).toBeTruthy();
  });
});
